# The worker itself needs only the Python standard library, so pairing and
# reporting in work on a plain Python install. Video generation on this machine
# uses the pinned stack in ../wan22/requirements.txt.
